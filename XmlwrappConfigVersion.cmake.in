set(PACKAGE_VERSION @XMLWRAPP_VERSION@)

# CHeck whether the requirest PACKAGE_FIND_VERSION is compatible
if("${PACKAGE_VERSION}" VERSION_LESS "${PACKAGE_FIND_VERSION}")
  set(PACKAGE_VERION_COMPATABLE FALSE)
else()
  set(PACKAGE_VERION_COMPATABLE TRUE)
  if("${PACKAGE_VERSION}" VERSION_EQUAL "${PACKAGE_FIND_VERSION}")
    set(PACKAGE_VERSION_EXACT TRUE)
  endif()
endif()
