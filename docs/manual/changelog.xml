<appendix id="changelog"><title>Revision History</title>
    <!--################################################################-->
    <section id="ck-050"><title>Version 0.5.0</title>
	<subtitle>??</subtitle>

	<itemizedlist>
	    <listitem>
		<para>
		    Fixed a null pointer dereference problem in xml::attributes, Bug ID 20. Thanks
		    goes to John K. Hohm for finding this and providing a working patch.
		</para>
	    </listitem>

	    <listitem>
		<para>
		    Major clean ups in the test harness. Test code should be a lot easier to write
		    now.
		</para>
	    </listitem>
	</itemizedlist>
    </section>
    <!--################################################################-->
    <section id="ck-044"><title>Version 0.4.4</title>
	<subtitle>October 29, 2003</subtitle>

	<itemizedlist>
	    <listitem>
		<para>
		    Small changes so that xmlwrapp can work with libxml2 version 2.6.0. Thanks goes
		    to Craig Wiesen and Jonathan Wakely for finding this. (Bug ID 18).
		</para>
	    </listitem>

	    <listitem>
		<para>
		    Fixed a typo in the manual. Thanks goes to Bill Luoma. (Bug ID 16).
		</para>
	    </listitem>
	</itemizedlist>
    </section>
    <!--################################################################-->
    <section id="ck-043"><title>Version 0.4.3</title>
	<subtitle>August 19, 2003</subtitle>

	<itemizedlist>
	    <listitem>
		<para>
		    Fixed a build problem on Linux (bug 10). This happens when libxslt was installed
		    in /usr. configure.pl could not find libxslt because xslt-config does not list
		    /usr/include.
		</para>
	    </listitem>

	    <listitem>
		<para>
		    Updated the TODO list (<xref linkend="todo"/>).
		</para>
	    </listitem>
	</itemizedlist>
    </section>
    <!--################################################################-->
    <section id="ck-042"><title>Version 0.4.2</title>
	<subtitle>June 20, 2003</subtitle>

	<para>
	    Version 0.4.2 is a maintenance release. A few small bugs were fixed and a work around for
	    a bug in Perl 5.8 was added.
	</para>

	<itemizedlist>
	    <listitem>
		<para>
		    Updated Perl scripts to work around a bug in the Perl 5.8 regex parser. Thanks
		    goes to Andy Chou.
		</para>
	    </listitem>

	    <listitem>
		<para>
		    Added an encoding patch from Dmitriy Nikitinskiy.
		</para>
	    </listitem>
	</itemizedlist>
    </section>
    <!--################################################################-->
    <section id="ck-041"><title>Version 0.4.1</title>
	<subtitle>April 07, 2003</subtitle>

	<para>
	    Version 0.4.1 fixes a few small bugs and also includes some new features. It is
	    binary compatible with version 0.4.0.
	</para>

	<itemizedlist>
	    <listitem>
		<para>
		    Typos in the doxygen documentation were fixed thanks to Jonathan Wakely, Stephen
		    Blake and Chris de Hoop.
		</para>
	    </listitem>

	    <listitem>
		<para>
		    Fixed a bug in configure.pl that caused a bad xmlwrapp-config to be generated
		    when XSLT was enabled.
		</para>
	    </listitem>

	    <listitem>
		<para>
		    Added support for pkg-config and the xmlwrapp.pc file.
		</para>
	    </listitem>

	    <listitem>
		<para>
		    Added support for libexslt.
		</para>
	    </listitem>

	    <listitem>
		<para>
		    Added the <function>xml::init::remove_whitespace</function> member function to
		    skip ignorable whitespace from parsed XML documents. Default value is false, so
		    whitespace is included in the node tree by default.
		</para>
	    </listitem>

	    <listitem>
		<para>
		    Added the <function>xml::node::self</function> and
		    <function>xml::node::parent</function> member functions. They return
		    <classname>xml::node::iterator</classname> or
		    <classname>xml::node::const_iterator</classname> objects.
		</para>
	    </listitem>

	    <listitem>
		<para>
		    Added a new version of the <function>xml::node::insert</function> member function
		    that does not require a location iterator, but instead inserts at the end of the
		    child list like the <function>xml::node::push_back</function> member function
		    does.
		</para>
	    </listitem>

	    <listitem>
		<para>
		    Added a range version of the <function>xml::node::erase</function> member
		    function and a version that takes the name of a node to erase. All nodes with
		    matching names will be removed from the child list.
		</para>
	    </listitem>

	    <listitem>
		<para>
		    Added two versions of the <function>xml::node::sort</function> member function.
		    The first, will sort child nodes with the given name using one of their
		    attributes as a sort key. The other, uses a user supplied function object to
		    sort the child nodes.
		</para>
	    </listitem>
	</itemizedlist>
    </section>
    <!--################################################################-->
    <section id="ck-040"><title>Version 0.4.0</title>
	<subtitle>February 03, 2003</subtitle>

	<para>
	    Version 0.4.0 is a feature release. xmlwrapp now supports XSLT via libxslt. This support
	    is called xsltwrapp and is optional.
	</para>

	<itemizedlist>
	    <listitem>
		<para>
		    Added the xslt::init and xslt::stylesheet classes to support XSLT.
		</para>
	    </listitem>

	    <listitem>
		<para>
		    Added the xml::init::load_external_subsets member function to make libxml load
		    external subsets by default.
		</para>
	    </listitem>
	</itemizedlist>
    </section>
    <!--################################################################-->
    <section id="ck-030"><title>Version 0.3.0</title>
	<subtitle>January 07, 2003</subtitle>

	<para>
	    This version contains a lot of new features. Because of this, certain API calls have
	    been changed or removed. The following list should help you update code that is using
	    xmlwrapp.
	</para>

	<itemizedlist>
	    <listitem>
		<para>
		    Changed configure.pl to require at least version 2.4.28 of libxml2.
		</para>
	    </listitem>

	    <listitem>
		<para>
		    The xml::tree_parser constructors can now throw exceptions if there was an error
		    during parsing. This is optional, and is controlled by a bool flag passed to the
		    constructors. This also means that xml::tree_parser will now prevent libxml2
		    from sending error messages to standard error. The new default behavior is to
		    throw an exception.
		</para>
	    </listitem>

	    <listitem>
		<para>
		    Some of the xml::event_parser callback member functions have changed. There are
		    also new callbacks so that you can catch CDATA sections, processing instructions
		    and XML comments.
		</para>
	    </listitem>

	    <listitem>
		<para>
		    It is now safe to throw exceptions from within an xml::event_parser callback.
		    The exception will not be passed up the stack to the caller of one of the
		    parsing member functions. Instead, it will stop the parsing and set an error
		    condition.
		</para>
	    </listitem>

	    <listitem>
		<para>
		    There is a new xml::document class that allows you to set XML document variables
		    such as the encoding and version. The document class also supports saving itself
		    to a file, string or a std::ostream. It also includes functions for validating a
		    document against an internal or external DTD.
		</para>
	    </listitem>

	    <listitem>
		<para>
		    The xml::tree_parser class no longer contains a get_root_node() member function.
		    It has been replaced with get_document() which will return a reference to a
		    xml::document object. That object can then be used to call get_root_node().
		</para>
	    </listitem>

	    <listitem>
		<para>
		    Another new class, xml::attributes, for getting and setting the attributes of a
		    node. This class replaces all the member functions of xml::node that dealt with
		    attributes. This class is much better than using the old xml::node member
		    functions because it supports iterators and functions like find.
		</para>
	    </listitem>

	    <listitem>
		<para>
		    The xml::init class will prevent libxml2 from sending any messages to stderr.
		    This should not be a problem since most classes will catch the message and use
		    it in an exception or store it for later use.
		</para>
	    </listitem>

	    <listitem>
		<para>
		    xml::init will now set some default libxml2 global variables. Indenting of
		    output XML text is turned on. Default substitution of entities on turned on.
		    Validating of all documents by default is turned off. There are new member
		    functions you can use to change these defaults.
		</para>
	    </listitem>

	    <listitem>
		<para>
		    Compiler flags will no longer contain quotes around directory names. This was
		    added for cygwin when people have a space in the current working directory path.
		    It was removed because it was causing problems on other platforms.
		</para>
	    </listitem>

	    <listitem>
		<para>
		    There is a new header file, xmlwrapp/xmlwrapp.h, that includes all of the other
		    xmlwrapp header files.
		</para>
	    </listitem>

	    <listitem>
		<para>
		    A real working test harness has been added with several tests.
		</para>
	    </listitem>

	    <listitem>
		<para>
		    New member functions for xml::node.

		    <itemizedlist>
			<listitem>
			    <para>
				4 different versions of xml::node::find.
			    </para>
			</listitem>

			<listitem>
			    <para>
				xml::node::insert.
			    </para>
			</listitem>

			<listitem>
			    <para>
				xml::node::replace.
			    </para>
			</listitem>

			<listitem>
			    <para>
				xml::node::erase.
			    </para>
			</listitem>

			<listitem>
			    <para>
				xml::node::get_attributes.
			    </para>
			</listitem>

			<listitem>
			    <para>
				xml::node::get_type.
			    </para>
			</listitem>
		    </itemizedlist>
		</para>
	    </listitem>
	</itemizedlist>
    </section>
    <!--################################################################-->
    <section id="ck-022"><title>Version 0.2.2</title>
	<subtitle>September 03, 2002</subtitle>

	<para>
	    Version 0.2.2 was a bug fix release.
	</para>

	<itemizedlist>
	    <listitem>
		<para>
		    Changed include guards so that they don't violate the
		    C++ standard. Thanks to Greg Chicares.
		</para>
	    </listitem>

	    <listitem>
		<para>
		    Include &lt;cstddef&gt; to get std::size_t and
		    std::ptrdiff_t. Thanks to Greg Chicares.
		</para>
	    </listitem>

	    <listitem>
		<para>
		    Fix a major bug where a pimpl was not created in
		    xml::tree_parser. Thanks to Greg Chicares.
		</para>
	    </listitem>

	    <listitem>
		<para>
		    Added a call to xmlKeepBlanksDefault(0) in xml::init
		    constructor to produce better looking XML. If this
		    causes any problems we will have to remove it. This was
		    suggested by Daniel Evison.
		</para>
	    </listitem>

	    <listitem>
		<para>
		    Fixed an issue with command quoting in the configure.pl
		    script.
		</para>
	    </listitem>
	</itemizedlist>
    </section>
    <!--################################################################-->
    <section id="cl-021"><title>Version 0.2.1</title>
	<subtitle>June 26, 2002</subtitle>

	<para>
	    Version 0.2.1 was a bug fix release.
	</para>

	<itemizedlist>
	    <listitem>
		<para>
		    Fixed a bug in the xml::event_parser class that caused
		    parsing to continue even when one of the event member
		    functions returned false. Thanks to Michael Grabner for
		    finding this one.
		</para>
	    </listitem>

	    <listitem>
		<para>
		    There were a few reports that xmlwrapp could be compiled
		    using MSVC on Win32. A project file for MSVC was added
		    so that no one would have to do this again. The STLport
		    is needed on the Win32 platform.
		</para>
	    </listitem>

	    <listitem>
		<para>
		    The get_namespace and set_namespace functions were
		    removed from the xml::node class since they were defined
		    but not yet implemented.
		</para>
	    </listitem>
	</itemizedlist>
    </section>
    <!--################################################################-->
    <section id="cl-020"><title>Version 0.2.0</title>
	<subtitle>May 03, 2002</subtitle>

	<para>
	    Version 0.2.0 of xmlwrapp was the first public release. It
	    included the following changes from version 0.1.0.
	</para>

	<itemizedlist>
	    <listitem>
		<para>
		    First draft of the documentation.
		</para>
	    </listitem>

	    <listitem>
		<para>
		    Added example programs.
		</para>
	    </listitem>

	    <listitem>
		<para>
		    Changed operator&lt;&lt; for xml::node from a template
		    function to a normal function that takes a std::ostream.
		    This allows xmlwrapp to compile with GCC &lt; 3.0.
		</para>
	    </listitem>

	    <listitem>
		<para>
		    Replaced calls to std::free with xmlFree in the libxml2
		    backend wrapper.
		</para>
	    </listitem>
	</itemizedlist>
    </section>
    <!--################################################################-->
    <section id="cl-010"><title>Version 0.1.0</title>
	<subtitle>April 11, 2002</subtitle>

	<para>
	    Version 0.1.0 was the first packaged version. It was packaged for
	    <ulink url="http://pmade.org/software/clo++/">clo++</ulink>.
	</para>

	<para>
	    It included no documentation or examples, and was not intended
	    for use by other developers.
	</para>
    </section>
    <!--################################################################-->
</appendix>
<!--
    vim:ft=docbk:tw=100
-->
