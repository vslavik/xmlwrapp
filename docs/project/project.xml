<?xml version="1.0"?>
<project xmlns="http://pmade.org/xmlns/project-main" name="xmlwrapp">
    <variables>
	<variable name="download-source"    value="xmlwrapp-0.4.4.tar.gz"/>
	<variable name="logo-image"	    value="xmlwrapp-logo.jpg"/>
	<variable name="email-address"	    value="software@pmade.org"/>
	<variable name="website"	    value="http://pmade.org/software/xmlwrapp/"/>
    </variables>

    <description>
	<para>
	    xmlwrapp is a modern style C++ library for working with XML
	    data. It provides a simple and easy to use interface for the
	    very powerful libxml2 XML parser and the libxslt XSLT engine.
	</para>
    </description>

    <features>
	<feature>
	    Tree parsing. XML data is parsed and a tree of xml::node
	    objects is created. Similar to the DOM.
	</feature>

	<feature>
	    Event parsing. XML data is parsed as protected member
	    functions of an event class are called. Similar to SAX.
	</feature>

	<feature>
	    It is easy to construct an XML tree using xml::node objects.
	    Any xml::node may be inserted into an IOStream causing
	    translation to XML text data.
	</feature>

	<feature>
	    Support for Extensible Stylesheet Language Transformations (XSLT)
	    and Extensions to XSLT (EXSLT).
	</feature>

	<feature>
	    Complete isolation from the backend parser due to the private
	    implementation (pimpl) idiom.
	</feature>

	<feature>
	    xmlwrapp is released under a Berkeley style license. This means
	    you can do almost anything with it, including using it in
	    commercial applications.
	</feature>
    </features>

    <section title="news"	file="news.xml"/>
    <section title="resources"	file="resources.xml"/>
    <section title="download"	file="download.xml"/>
    <section title="license"	link="download/documentation/manual/sep-html/license.html"/>
    <section title="cvs"	link="http://cvs.pmade.org/view/xmlwrapp/"/>
</project>
